#ifndef _HASH_H
#define _HASH_H

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

typedef struct HashNode
{
	int value;
	int key;
	struct HashNode *next;
}HashNode;

typedef struct HashTable
{
	
}HashTable;

#endif
